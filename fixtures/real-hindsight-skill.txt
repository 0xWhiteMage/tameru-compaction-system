{"success": true, "name": "hindsight-memory-migration", "description": "Migrate Hermes memory provider from Honcho to Hindsight (Vectorize.io), running fully local on NAS.", "tags": ["hindsight", "honcho", "memory", "migration", "nas"], "related_skills": [], "content": "---\nname: hindsight-memory-migration\nversion: 1.0.0\ndescription: Migrate Hermes memory provider from Honcho to Hindsight (Vectorize.io), running fully local on NAS.\ntags: [hindsight, honcho, memory, migration, nas]\n---\n\n# Hindsight Memory Migration — Replace Honcho with Hindsight\n\nMigrate Hermes memory provider from Honcho to Hindsight (by Vectorize.io), running fully local on NAS.\n\n## Audit note — current live deployment snapshot (2026-05-04)\n\nThe currently verified live setup on this machine is:\n- Hermes home: `/volume2/Hailey/Hermes/home/.hermes`\n- Hermes repo used by the Hindsight service: `/volume2/Hailey/Hermes/repo`\n- Service manager: `hindsight-external.service` via systemd\n- Listen URL: `http://127.0.0.1:8888`\n- Current database target from live `ExecStart`: `postgresql://paperclip:***@127.0.0.1:54329/hindsight`\n\nSome older sections below mention earlier migration paths, `/volume1/...` locations, or previous database ports. Treat the live systemd unit plus current config files as canonical when they disagree with historical notes.\n\n## Status: Deployed — local_external mode, systemd-persistent, native MCP tools fixed\n\n**Last verified: 2026-08-08.** External daemon on port 8888 runs under systemd (`hindsight-external.service` active + enabled, survives reboots). Versions: `hindsight-api` 0.8.6 (server) + `hindsight-client` 0.9.0 (client; upgraded from 0.6.1 on 2026-08-08 — verify plugin still loads cleanly after any client bump; it does as of 0.9.0). **hermes-lcm v0.19.0 now runs alongside as the Hermes context engine** (`context.engine: lcm`, store at `~/.hermes/lcm.db`) — memory provider (hindsight) and context engine (lcm) are separate config axes and coexist fine. LLM provider is **DeepSeek v4 Flash** via `openai` provider at `https://api.deepseek.com/v1` — switched after both MiniMax (429 quota exhausted) and Xiaomi Mimo (401 invalid key) sequentially failed. Vector extension loads correctly. `.env` and profile env (`~/.hindsight/profiles/hermes.env`) are in sync. Native `hindsight_recall` and `hindsight_retain` confirmed working.\n\n**Post-reboot recovery:** The canonical fix is the systemd dependency chain: `paperclip-pg.service` starts PG on port 54329, then `hindsight-external.service` starts Hindsight (see \"Systemd Boot Dependency\" below). If systemd isn't available, fall back to `scripts/start-hindsight-boot.sh` which starts PG then Hindsight. For ad-hoc restarts when PG is already running, use `scripts/start-hindsight.sh`. Systemd will auto-start via `hindsight-external.service` on subsequent reboots (override at `/etc/systemd/system/hindsight-external.service.d/override.conf` sets `LD_LIBRARY_PATH`).\n\n**Quick provider consistency check (run after any LLM provider switch):**\n```bash\npython3 -c \"\nimport os\ndef rv(path, var):\n    try:\n        with open(path, 'rb') as f:\n            for line in f:\n                if line.startswith(var.encode() + b'='):\n                    return line.split(b'=', 1)[1].decode().strip()\n    except: pass\n    return None\n\nenv = os.path.expanduser('~/.hermes/.env')\nprofile = os.path.expanduser('~/.hindsight/profiles/hermes.env')\nprint(f'.env base_url:  {rv(env, \\\"HINDSIGHT_API_LLM_BASE_URL\\\")}')\nprint(f'prof base_url:  {rv(profile, \\\"HINDSIGHT_API_LLM_BASE_URL\\\")}')\nprint(f'.env key match: {rv(env, \\\"HINDSIGHT_LLM_API_KEY\\\") == rv(profile, \\\"HINDSIGHT_API_LLM_API_KEY\\\")}')\n\"\n```\nIf the base URLs or keys don't match, the `.env` is stale and will revert the profile on next regeneration.\n\n## 🔍 Quick Diagnosis: Hindsight Is Down\n\nHindsight has gone down repeatedly (June, July 2026). Before reading the 130+ pitfalls below, run this 3-minute triage flow.\n\n### Step 1 — Check the three things (in this order)\n\n```bash\nsystemctl is-active hindsight-external.service          # 1a\nss -tlnp | grep ':8888 '                                # 1b\ncurl -sf http://127.0.0.1:8888/health                   # 1c\n```\n\n| 1a result | 1b result | 1c result | Likely root cause | Jump to |\n|-----------|-----------|-----------|-------------------|---------|\n| `inactive` | empty | connection refused | LLM provider died on last startup | Step 2 |\n| `activating` (auto-restart) | empty | connection refused | LLM provider 429/401 OR PG down | Step 2 |\n| `active` | empty | connection refused | PG down OR port conflict | Step 3 |\n| `active` | `:8888` | healthy / `database: connected` | It's fine — check `hindsight_recall` tool or REST API | Done |\n\n### Step 2 — Read the log (pinpoint the error class)\n\n```bash\ntail -40 ~/.hermes/logs/hindsight-external.log\n```\n\nError signatures in the log and their root causes:\n\n| Log line | Root cause | Fix |\n|----------|-----------|-----|\n| `RuntimeError: Connection verification failed for ... Error code: 429` | LLM provider quota exhausted | Top up credits OR switch provider |\n| `RuntimeError: Connection verification failed for ... Error code: 401` | LLM API key invalid | Rotate key OR switch provider |\n| `FATAL: password authentication failed for user \"paperclip\"` | DB password wrong | Check `~/.hindsight/profiles/hermes.env` and systemd unit for the actual URL |\n| `FATAL: could not access file \"vector\"` | pgvector not loaded | Fix A (dynamic_library_path), Fix B (LD_LIBRARY_PATH), or Fix C (PG down) |\n| `FATAL: data directory has invalid permissions` | PG data dir mode not 700 | `chmod 700` on PG data dir |\n| `Database migration failed` | PG down or schema issue | Check PG on 54329 first |\n| `the database system is in recovery mode` | PG was killed uncleanly | Wait 30s or `pg_ctl -D .../db stop -m immediate && pg_ctl start` |\n\n### Step 3 — For LLM provider failures (most common in 2026)\n\nLLM provider issues cause an **instant crash at startup** — no port, no health, just an ERROR log. The profile env (`~/.hindsight/profiles/hermes.env`) is the daemon's source of truth. The cascading provider failure chain documented July 2026 was:\n\n1. MiniMax → `429` (quota exhausted)\n2. Xiaomi Mimo → `401` (key expired/invalid)\n3. **DeepSeek → `200 OK`** (still working)\n\nWhen a provider fails, verify the next candidate directly before updating all 4 config files:\n\n```bash\ncurl -s -H \"Authorization: Bearer $KEY\" $BASE_URL/chat/completions \\\n  -d '{\"model\":\"$MODEL\",\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}],\"max_tokens\":10}'\n```\n\nThe four files that must ALL be updated when switching providers:\n- `$HERMES_HOME/.env` (plugin-side `HINDSIGHT_LLM_API_KEY` + `HINDSIGHT_API_LLM_BASE_URL`)\n- `~/.hindsight/profiles/hermes.env` (daemon-side `HINDSIGHT_API_LLM_API_KEY`, `HINDSIGHT_API_LLM_MODEL`, `HINDSIGHT_API_LLM_BASE_URL`)\n- `$HERMES_HO
[...omitted...]
k — if they do, this is a permissions issue, not an installation issue:\n  ```bash\n  ls /usr/share/postgresql/*/extension/vector* 2>/dev/null | head -5\n  # If files exist here, the problem is DB-level, not filesystem-level\n  ```\n\n  **Fix for permissions variant:** The `paperclip` user needs `USAGE` on the `vector` extension schema. Run as postgres superuser:\n  ```sql\n  -- Grant extension access to paperclip user\n  GRANT USAGE ON SCHEMA extensions TO paperclip;\n  GRANT CREATE ON SCHEMA extensions TO paperclip;\n  -- Or if the extension lives in the public schema:\n  GRANT USAGE ON SCHEMA public TO paperclip;\n  GRANT ALL PRIVILEGES ON DATABASE hindsight TO paperclip;\n  ```\n\n  **If even that fails** (no postgres superuser accessible): Write to file as fallback — save critical memory to `/volume2/Hailey/Hermes/workspace/hindsight-backup-<date>.md` as tagged Markdown sections. This is what this session used when `hindsight_retain` was completely blocked.\n\n  **Why this is different from the pgvector installation fix:** The installation fix addresses *files missing from disk* (port 55444, `$libdir` path mismatch). This permissions variant happens when files are present but the DB user lacks schema/extension privileges. Both produce the same error message, so distinguish by checking for the files first.\n- **PG data dir mode 777 silently breaks PostgreSQL startup.** `pg_ctl start` succeeds but the postmaster immediately exits with `FATAL: data directory has invalid permissions`. PostgreSQL requires `0700` (or `0750`), not `0777`. Fix: `chmod 700 <data_dir>`. The `paperclip-pg.service` unit includes `ExecStartPre=/bin/chmod 700` as a safeguard.\n- **Missing `port` in postgresql.conf causes silent port 5432 conflict.** Without an explicit `port = 54329` line, PG defaults to 5432 — which is already taken by the system PostgreSQL. `pg_ctl start` reports success but the port is on IPv6 only, and Hindsight connections to 127.0.0.1:54329 fail with \"Connection refused\". Always verify `grep \"^port\" postgresql.conf`.\n- **`kill <pid>` (SIGTERM) doesn't reliably stop Paperclip PG.** The postmaster may ignore SIGTERM, leaving the process alive and `postmaster.pid` on disk. The systemd service then fails with \"lock file already exists\". Fix: use `kill -9` then `rm -f postmaster.pid` before starting.\n- **`systemctl --user` is broken on this NAS** (\"No medium found\" — D-Bus session bus socket missing). All Hindsight/Paperclip service management must use `sudo systemctl` (system instance). Do not attempt user-level systemd for these services. Hermes' embedded mode materialises the profile env from the plugin config and `$HERMES_HOME/.env` (`HINDSIGHT_LLM_API_KEY`), while the standalone profile env lives at `~/.hindsight/profiles/<profile>.env`. Before switching from `local_external` back to `local_embedded`, compare the live plugin config JSON, `$HERMES_HOME/.env`, and the profile env file. Regenerate the profile env from the canonical Hermes config rather than trusting an old profile file that may contain a stale or invalid key.\n- **The most dangerous embedded cutover bug is empty-value override.** Even if `~/.hindsight/profiles/<profile>.env` contains a valid `HINDSIGHT_API_LLM_API_KEY`, embedded startup can still fail if Hermes/plugin runtime config passes `llm_api_key=\"\"` or similar empty values and the embed manager merges `{**profile_config, **config}` naively. In that shape, the explicit empty runtime value clobbers the good profile credential, the daemon child env is spawned without `HINDSIGHT_API_LLM_API_KEY`, and startup dies with `ValueError: LLM API key is required` or a misleading timeout. The safe merge rule is: **empty runtime values must NOT override non-empty profile values**.\n- **Verify credential propagation at the child env boundary, not just in files.** Seeing a valid key in `$HERMES_HOME/.env` and `~/.hindsight/profiles/<profile>.env` is not enough. Add temporary daemon spawn debug or inspect the actual child process env/log to confirm `HINDSIGHT_API_LLM_API_KEY` reaches the spawned `hindsight_api.main` process with a non-zero length. This distinguishes `file is correct but manager dropped it` from `credential is actually wrong`.\n- **A working direct MiniMax/OpenAI-compatible call can prove the key is fine while embedded startup is still broken.** If direct `/models` or `chat.completions.create(...)` calls succeed with the same key but embedded Hindsight still fails, stop rotating credentials. The issue is probably env propagation/merge logic in the embedded manager or plugin, not the key itself.\n- **Do not retire `local_external` until real embedded operations pass.** Service status and daemon health are not enough. The cutback to `local_embedded` is only complete after a real provider-path verification shows `mode=local_embedded`, `_get_client()` returns `HindsightEmbedded`, and at least one real retain/recall operation succeeds. Only then disable the external workaround service.\n- **If `~/
[...omitted...]
 `/volume2/Hailey/Hermes/repo/venv/bin/python3` | `/volume2/Hailey/Hermes/repo/venv/` | hermes | **INSTALLED** |\n| Terminal / user sessions | `/usr/bin/python3.11` (system) | repo venv via activate | hermes | **INSTALLED** |\n\n**The critical failure mode:** The gateway's `hindsight_client` import fails because its venv (`/usr/local/lib/hermes-agent/venv/`) is root-owned and has `hindsight_client` NOT installed. The fix is **not** `pip install hindsight-all -U` (you can't write to that venv). The fix options are:\n1. **Preferred:** Add `PYTHONPATH` env var to the systemd service so the gateway reads from the repo venv (which already has `hindsight_client`)\n2. **Alternative:** `sudo /usr/local/lib/hermes-agent/venv/bin/pip install hindsight-client` (requires sudo access)\n3. **Alternative:** Copy/symlink `hindsight_client` package from repo venv to gateway venv (requires root)\n\n**Step 7 — Check for existing hindsight processes before proposing new installs**\n```bash\nps aux | grep -E \"hindsight_api|hindsight\" | grep -v grep\nss -tlnp | grep -E \"8888|9177|5435\"\n```\nYou may have a stale crashed daemon that needs killing, not a new install.\n\n**Golden rule:** If you are about to propose `pip install` for anything related to Hermes memory, infrastructure, or gateway components — always run the audit checklist above first. At minimum, identify which venv the target process actually uses before opening a terminal.\n\n## Support files\n\n- `scripts/start-hindsight.sh` — verified working launcher. Sets `LD_LIBRARY_PATH` + sources profile env + starts Hindsight on port 8888. Run: `bash /volume2/Hailey/Hermes/home/.hermes/scripts/start-hindsight.sh` (or background with `&> ...log 2>&1 &`). Assumes PG is already running.\n- `scripts/start-hindsight-boot.sh` — boot-time launcher. Starts PG (fix permissions, wait for readiness), then starts Hindsight. Used as fallback when systemd isn't reachable.\n- `scripts/hind_daemon.py` — older daemon launcher using `python3 -S` to bypass `sitecustomize.py` redaction. Run as `hermes` user via tmux.\n- `templates/paperclip-pg.service` — systemd unit for Paperclip's embedded PostgreSQL on port 54329. Install with `sudo cp ... /etc/systemd/system/` and wire Hindsight to depend on it.\n- `references/vector-ld-library-path-2026-05-14.md` — LD_LIBRARY_PATH root cause for runtime vector loading failure on port 54329. Full diagnostics, fix steps, three-variant comparison table, and the kill-and-restart trick.\n- `references/multi-venv-environment-map.md` — full environment map of the NAS multi-venv architecture, including gateway venv isolation findings and the `hindsight_client` import failure root cause.\n- `references/native-tools-fix-2026-04-29.md` — concrete verification path for the native Hindsight tool fix and the focused-test isolation cleanup.\n- `references/hindsight-launch-and-vector-fix-2026-05-14.md` — **LD_LIBRARY_PATH variant** (vector.so on disk but linker can't find it) + **background vs foreground launch** failure pattern. Full diagnostic checklist, fix commands, and the \"pgvector is available\" diagnostic marker.\n- `references/mimo-v25-provider-migration-2026-06-11.md` — Mimo v2.5 provider switch from MiniMax M2.7. Covers correct base URL, model availability on token-plan, config file changes, key extraction, and revert steps.\n- `references/hindsight-down-diagnosis.md` — Compact diagnostic flowchart with the July 2026 cascading provider failure worked example (MiniMax 429 → Xiaomi 401 → DeepSeek). Use when Hindsight is down and you need a step-by-step triage.\n\n## Reference URLs\n\n- Docs: https://hindsight.vectorize.io/\n- GitHub: https://github.com/vectorize-io/hindsight\n- Paper: https://arxiv.org/abs/2512.12818\n- Benchmarks: https://benchmarks.hindsight.vectorize.io/\n", "path": "devops/hindsight-memory-migration/SKILL.md", "skill_dir": "/volume2/Hailey/Hermes/home/.hermes/skills/devops/hindsight-memory-migration", "org_provenance": null, "linked_files": {"references": ["references/multi-venv-environment-map.md", "references/mimo-v25-provider-migration-2026-06-11.md", "references/hindsight-backup-20260513.md", "references/vector-ld-library-path-2026-05-14.md", "references/hindsight-down-diagnosis.md", "references/config-precedence-and-bank-splitting.md", "references/hindsight-launch-and-vector-fix-2026-05-14.md", "references/native-tools-fix-2026-04-29.md"], "scripts": ["scripts/hind_daemon.py", "scripts/start-hindsight-boot.sh", "scripts/start-hindsight.sh"]}, "usage_hint": "To view linked files, call skill_view(name, file_path) where file_path is e.g. 'references/api.md' or 'assets/config.yaml'", "required_environment_variables": [], "required_commands": [], "missing_required_environment_variables": [], "missing_credential_files": [], "missing_required_commands": [], "setup_needed": false, "setup_skipped": false, "readiness_status": "available", "_source_path": "/volume2/Hailey/Hermes/home/.hermes/skills/devops/hindsight-memory-migration/SKILL.md"}